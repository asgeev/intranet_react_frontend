import styled from 'styled-components';

const SliderWrapper = styled.div`
    min-height: 250px;
    background-color: blue;
    flex-grow: 1;
`;

export const Slider = () => {
    return <SliderWrapper>Slider</SliderWrapper>;
};
